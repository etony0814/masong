# 03_開發日志

> 項目名稱：狗狗生活日誌網
> 啟動日期：2026-07-31
> 狀態：🟢 已完成（MVP）
> 最後更新：2026-07-31

---

## 開發進度

### 2026-07-31

- [x] 建立專案資料夾結構
- [x] 閱讀全局文檔並更新全局控制台
- [x] 撰寫需求文檔與技術棧文檔
- [x] 建立後端伺服器（Express + sql.js + multer）
- [x] 建立前端頁面（HTML/CSS/JS，響應式設計）
- [x] 實作照片上傳與瀏覽功能
- [x] 實作影片上傳與播放功能
- [x] 實作生活記錄 CRUD
- [x] 實作時間軸展示
- [x] 實作里程碑管理
- [x] 修復 sql.js lastInsertRowid 問題
- [x] 修復 milestones 重複資料問題（DROP+CREATE 重建表）
- [x] 所有 API 端點測試通過

---

## 踩坑記錄

### #001 sql.js lastInsertRowid 在不同進程間重置
- **現象**：`db.exec('SELECT last_insert_rowid()')` 在新進程中回傳 0
- **原因**：sql.js 每次進程重新讀取 DB 檔案，SQLite session state 不持久化
- **解決方案**：在 `insertAndGetId()` 中，INSERT 後立即在同一進程內執行 `SELECT last_insert_rowid()`
- **預防措施**：使用 sql.js 時避免跨進程依賴 session state

### #002 milestones 重複資料
- **現象**：多次測試後 milestones 表有重複記錄
- **原因**：初始創建表時無 UNIQUE 約束，後期新增約束對已有資料無效
- **解決方案**：`DROP TABLE IF EXISTS` + 重新 `CREATE TABLE` 加上 `UNIQUE` 約束
- **預防措施**：開發階段先用空 DB 測試，或定期重建初始資料

---

## 專案結構

\`\`\`
Project20260731-001-狗狗生活日誌網/
├── frontend/
│   ├── index.html    # 主頁面（首頁/時間軸/照片牆/影片/新增記錄）
│   ├── style.css     # 響應式樣式（暖色調，寵物主題）
│   └── app.js        # 前端邏輯（導航/API/表單/Lightbox）
├── backend/
│   ├── server.js     # Express 伺服器 + API 路由
│   ├── database.js   # sql.js 資料庫模組
│   ├── package.json  # 依賴：express, sql.js, multer, cors
│   ├── data/         # SQLite 資料庫（自動建立）
│   └── uploads/      # 上傳檔案目錄
├── 00_項目控制台.md
├── 01_需求文档.md
├── 02_技術棧.md
├── 03_開發日志.md
└── README.md
\`\`\`

---

## 使用方式

\`\`\`bash
# 啟動後端
cd backend
npm start
# 開啟瀏覽器訪問 http://localhost:3000
\`\`\`
